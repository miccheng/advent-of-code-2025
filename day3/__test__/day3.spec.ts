import { describe, expect, test } from 'vitest'
import { solutionPart1 } from '../day3'

describe('Day3 Tests', () => {
    test('No actions', () => {
        expect(solutionPart1('')).toEqual({});
    })
})
